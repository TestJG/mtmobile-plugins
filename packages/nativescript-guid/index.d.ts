/**
 * @param separator If not supplied, default separator ("-") will be used.
 * @returns A compliant Guid created in native platform.
 */
export declare const createGuid: (separator?: string) => string;
